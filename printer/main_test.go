/*
package printer
import "testing"

	func TestPrintHello(t *testing.T) {
		got := PrinyerHello("Igor")
		expected := "Hello, Igor"
		if got != expected{
			t.Fatalf(`PrintHello("Igor") =%q, want %q`, got, expected)
		}
	}
*/
package printer

import "testing"

func TestPrintHello(t *testing.T) {
	t.Fail()
}
